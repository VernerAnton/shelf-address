/**
 * OpenCV.js, served from this site (scripts/copy-opencv.mjs puts it there).
 * The version is in the name so it can be cached for good; bump this together
 * with the @techstark/opencv-js version in package.json.
 */
export const OPENCV_URL = "/vendor/" + "opencv-4.12.0.js";
