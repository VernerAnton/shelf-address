import { ComingSoon } from "@/components/coming-soon";

export default function CatalogPage() {
  return (
    <ComingSoon
      title="Catalog"
      body="Searching for a book and getting its address back arrives once scanning and lookup are in (Phases 3–4)."
    />
  );
}
